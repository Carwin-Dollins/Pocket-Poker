To run the code:
Create a .env file with the SUPABASE_URL and the SUPABASE_KEY for the database. It should look something like this:

SUPABASE_URL=<url here>
SUPABASE_KEY=<key here>

Then install dependencies with the following command:
pip3 install -r requirements.txt

and then run the code as so:
python3 test.py